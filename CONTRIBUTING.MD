# Contributing Guidelines

Thanks for your interest in contributing to the Movie Search App!  
Follow the steps below to keep contributions organized and easy to review.

---

## 🧱 Project Setup

1. Fork the repo and clone your fork.
2. Create a new branch for your work:
   ```bash
   git checkout -b feat-12/add-search-api
   ```

3. Follow conventional branch prefixes:

   * `feat/` or `feat-#` — new feature
   * `fix/` — bug fix
   * `chore/` — config or maintenance task
   * `docs/` — documentation updates
   * `test/` — tests only

Example: `feat-07/add-autocomplete` or `fix-15/query-error`

---

## 🧩 Commit Message Style

Follow this format:

```
type(scope): short description
```

**Examples:**

* `feat(api): add multi-match search endpoint`
* `fix(ui): handle empty query gracefully`
* `chore: add Docker Compose for Elasticsearch`

---

## 🧪 Testing

* Run existing tests before pushing.
* Add tests for any new functionality.
* Ensure all lint checks pass.

---

## 🚀 Pull Requests

1. Create an issue describing the task.
2. Reference that issue in your PR (e.g., `Closes #12`).
3. Keep PRs focused and small.
4. Request review when done.

---

## 🛡 Code of Conduct

By contributing, you agree to follow the [Code of Conduct](CODE_OF_CONDUCT.md).
